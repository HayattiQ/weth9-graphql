{
  "name": "weth9-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ weth9-graph",
    "create-local": "graph create --node http://${GRAPH_LOCAL_IP}:8020/ weth9-graph",
    "remove-local": "graph remove --node http://${GRAPH_LOCAL_IP}:8020/ weth9-graph",
    "deploy-local": "graph deploy --node http://${GRAPH_LOCAL_IP}:8020/ --ipfs http://${GRAPH_LOCAL_IP}:5001 weth9-graph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.65.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
